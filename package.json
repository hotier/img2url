{
  "name": "img2url-client",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && mkdir -p dist/functions && cp -r functions/* dist/functions/",
    "preview": "vite preview",
    "deploy": "npm run build && wrangler pages deploy dist",
    "dev:worker": "wrangler dev"
  },
  "dependencies": {
    "highlight.js": "^11.11.1",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^7.13.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.1",
    "vite": "^5.3.1",
    "wrangler": "^3.0.0"
  }
}
name: Deploy to Cloudflare

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      
      - name: Install dependencies
        run: npm install
      
      - name: Fix file names
        run: |
          cd client
          # 检查并修复可能的文件名问题
          if [ -f "vite。config。js" ]; then
            mv "vite。config。js" vite.config.js
          elif [ -f "vite.config.js" ]; then
            echo "File name is already correct: vite.config.js"
          else
            echo "No vite.config.js file found"
          fi
          ls -la
      
      - name: Build client
        run: npm run deploy:client
      
      - name: Deploy worker
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          workingDirectory: worker
          command: deploy
      
      - name: Deploy client to Pages
        uses: cloudflare/pages-action@v1
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          projectName: img2url
          directory: client/dist
          branch: main
